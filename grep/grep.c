#include "regex.h"
#include "stdio.h"

int main() {
  // char *line = "Hello world";
  // regex_t re;
  // regcomp(&re, "pattern", REG_ICASE);
  // int result = regexec(&re, line, 0, NULL, 0);
  //
  // printf("%d\n", result);

  return 0;
}
